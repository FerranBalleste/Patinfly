<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- app_bar_drawer.xml -->
    <!-- Base application theme. -->
    <style name="Theme.Patinfly" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_dark</item>
        <item name="colorOnPrimary">@color/color_text_on_p</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_dark</item>
        <item name="colorOnSecondary">@color/color_text_on_s</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorOnBackground">@color/main_text</item>
    </style>

    <style name="Theme.Patinfly.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- app_bar_drawer.xml -->
    <style name="Theme.Patinfly.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- app_bar_drawer.xml -->
    <style name="Theme.Patinfly.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>